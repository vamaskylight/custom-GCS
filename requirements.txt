# VGCS — Python dependencies (M0 baseline)
# Python 3.10+ recommended

pymavlink>=2.4.40,<3
pyserial>=3.5,<4
PySide6>=6.6.0,<7
watchdog>=4,<7

# M3 RTSP fallback decode (QtMultimedia may lack RTSP/H.264 support on Windows).
# We use an FFmpeg subprocess (no compiler needed). Numpy is used for fast frame handling.
numpy>=1.26,<3

# M8 observation reports — lat/lon to NATO grid reference (MGRS).
mgrs>=1.4,<2

# M8 optional GeoTIFF DEM (CSV/ESRI .asc work without rasterio).
rasterio>=1.3,<2

# M14 — software-side visual object tracking for continuous gimbal-follow
# (C12/C13 firmware has no confirmed continuous-follow capability of its own;
# see DOCS/SKYDROID-TOP-PROTOCOL.md). Needs the *contrib* build specifically —
# plain opencv-python(-headless) does not include the tracker algorithms
# (CSRT/KCF/MOSSE live in the contrib/legacy module).
opencv-contrib-python-headless>=4.9,<6